"use client";

/**
 * ThemeToggle — no-op.
 * The FitView AI design uses a fixed light editorial theme (#FAFAF8 cream),
 * so the theme toggle is intentionally removed from the UI.
 */

export default function ThemeToggle() {
  return null;
}
